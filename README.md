# hint-tracker-packs
Hint tracker packs for the bashprime hint tracker

## Building a Pack

You can build a pack by running `zip-pack.sh pack_directory`, which will zip the contents of the directory and save it in `dist/`.